#include<stdio.h>
int main()
{
    int count=0,i,n;
    printf("Enter range:");
    scanf("%d",&n);
    for(int i=2;i<n;i++)
    {
        if(n%i==0)
        {
            count++;
            break;
        }
        if(count==0)
        {
            printf("prime number ");
        }
        else
        {
           printf("not prime");
        }

    }

}
